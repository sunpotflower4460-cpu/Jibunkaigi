// src/runtime/buildMirrorSurfaceGuidance.js
// Mirror 専用の表層ガイダンス生成
// surfaceFrame を bridge データへ変換（自然文指示を削除）

// Phase II redo: Mirror への表層ガイダンスも LLM へは出さない。
export const buildMirrorSurfaceGuidance = (_surfaceFrame = {}) => {
  return '';
};
