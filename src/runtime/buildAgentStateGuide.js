// src/runtime/buildAgentStateGuide.js
// エージェント別の状態ガイド生成
// 各エージェントが「今回の user state にどう触れるべきか」を示す短いガイドを返す

// Phase II redo: 状態ガイドを LLM へ出さない。
export const buildAgentStateGuide = (_agentId, _estimatedState = {}) => {
  return '';
};
