// src/runtime/buildAgentInternalFrame.js
// エージェント別の内部フレーム生成
// internalOS を短ラベル中心で伝える（自然文削除）

// Phase II redo: 内部フレームを LLM へ出さない。
export const buildAgentInternalFrame = ({
  agentId: _agentId,
  internalOS: _internalOS,
  estimatedState: _estimatedState = {},
}) => {
  return '';
};
